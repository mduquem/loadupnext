import NavigationItems from './navigationItems/navigationItems';

const Navbar = (props) => (
   <nav>
      <div className='logo'>Load Up</div>
      <NavigationItems />
   </nav>
);
export default Navbar;
